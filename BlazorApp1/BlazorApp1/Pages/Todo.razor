@page "/todo"
@inject IJSRuntime JSRuntime

<h3>Todo(@todos.Count(todo => !todo.IsDone))</h3>

<ul>
    @foreach (var todo in todos)
    {
        <li>
            <input type="checkbox" @bind="todo.IsDone" />
            <input @bind="todo.Title" />
        </li>
    }
</ul>

<input placeholder="Something todo" @bind="newTodo" />
<button @onclick="AddTodo">Add todo</button>

<br/>
<br/>
<button @onclick="SayHello">全局函数</button>
<button @onclick="JsUtilSayHello">带范围的函数</button>
<button @onclick="SayHelloWithArg">带参数的函数</button>
<button @onclick="GetToDoItem">GetToDoItem</button>
<button @onclick="TestConfirm">TestConfirm</button>